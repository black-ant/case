========================================
ChatService 快速参考卡片
========================================

## 🚀 快速开始

```java
@Autowired
private ChatService chatService;

// 简单对话
String response = chatService.chat("你好");

// 流式对话
Flux<String> stream = chatService.streamChat("写一首诗");
```

## 📋 常用方法速查

### 基础对话
```java
// 1. 简单对话
chat("你好")

// 2. 流式对话
streamChat("写一首诗")
```

### 角色定制
```java
// 3. 自定义角色
chatWithSystem("你是一个专家", "问题")

// 4. 技术专家
chatAsTechExpert("什么是 Spring Boot？")

// 5. 翻译助手
translate("Hello", "中文")
```

### 参数控制
```java
// 6. 控制随机性 (0.0-2.0)
chatWithTemperature("问题", 0.7)

// 7. 限制长度
chatWithMaxTokens("问题", 500)

// 8. 完整配置
chatWithFullOptions("问题")
```

### 模型选择
```java
// 9. 指定模型
chatWithModel("问题", "gpt-4")

// 10. 快速模型 (gpt-3.5-turbo)
chatFast("问题")

// 11. 高级模型 (gpt-4)
chatAdvanced("问题")
```

### 特殊场景
```java
// 12. 创造性写作 (高 temperature)
creativeWriting("未来世界")

// 13. 精确回答 (低 temperature)
preciseAnswer("地球半径是多少？")

// 14. 代码生成
generateCode("实现单例模式")

// 15. 文本摘要
summarize("长文本...")
```

### 高级功能
```java
// 16. Prompt 对象
chatWithPromptObject("系统提示", "用户消息")

// 17. 获取元数据
Map<String, Object> result = chatWithMetadata("问题");
int tokens = (int) result.get("totalTokens");

// 18. 流式带配置
streamWithOptions("问题", "系统提示")

// 19. 多轮对话
chatWithContext("历史对话", "新消息")

// 20. 自定义客户端
chatWithCustomClient("问题")
```

## 🎯 参数速查表

| 参数 | 范围 | 说明 | 推荐值 |
|------|------|------|--------|
| temperature | 0.0-2.0 | 随机性 | 0.7 |
| maxTokens | 1-4096 | 输出长度 | 1000 |
| topP | 0.0-1.0 | 词汇多样性 | 0.9 |
| frequencyPenalty | -2.0-2.0 | 降低重复 | 0.5 |
| presencePenalty | -2.0-2.0 | 鼓励新话题 | 0.5 |

## 🎨 Temperature 使用指南

| 值 | 场景 | 示例 |
|----|------|------|
| 0.0-0.3 | 精确任务 | 事实问答、代码生成 |
| 0.7-0.9 | 通用对话 | 日常聊天、内容创作 |
| 1.0-2.0 | 创意任务 | 故事创作、头脑风暴 |

## 🔧 常见场景配置

### 代码生成
```java
chatWithTemperature("实现快速排序", 0.3)
// 或
generateCode("实现快速排序")
```

### 创意写作
```java
chatWithTemperature("写一个科幻故事", 1.2)
// 或
creativeWriting("科幻世界")
```

### 事实问答
```java
chatWithTemperature("地球的半径", 0.1)
// 或
preciseAnswer("地球的半径")
```

### 翻译
```java
translate("Hello World", "中文")
```

### 摘要
```java
summarize("长文本内容...")
```

## 💡 最佳实践

### 1. 选择合适的方法
- 简单任务 → chat()
- 长文本 → streamChat()
- 技术问题 → chatAsTechExpert()
- 代码 → generateCode()

### 2. 控制成本
- 使用 maxTokens 限制输出
- 选择合适的模型
- 缓存常见问题

### 3. 提升质量
- 使用明确的系统提示词
- 调整 temperature
- 提供足够的上下文

### 4. 优化性能
- 简单任务用 chatFast()
- 复杂任务用 chatAdvanced()
- 长文本用流式响应

## 🐛 常见问题

### Q: 回答不够准确？
```java
// 降低 temperature
chatWithTemperature("问题", 0.1)
```

### Q: 回答太死板？
```java
// 提高 temperature
chatWithTemperature("问题", 1.0)
```

### Q: 输出太长？
```java
// 限制 maxTokens
chatWithMaxTokens("问题", 300)
```

### Q: 需要实时反馈？
```java
// 使用流式响应
streamChat("问题")
```

### Q: 如何查看 token 使用？
```java
Map<String, Object> result = chatWithMetadata("问题");
System.out.println(result.get("totalTokens"));
```

## 📚 更多信息

- 完整指南: CHATSERVICE-GUIDE.txt
- 项目信息: PROJECT-INFO.txt
- 快速启动: QUICK-START.txt

========================================
保存此文件作为快速参考！
========================================
