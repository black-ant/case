# Spring AI Demo 环境变量配置示例
# 复制此文件为 .env 并填入你的配置

# ==================== OpenAI 配置 ====================
# OpenAI API Key（必填）
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI Base URL（可选，默认为官方地址）
# 如果使用代理或自定义端点，请修改此项
OPENAI_BASE_URL=https://api.openai.com

# OpenAI 模型（可选，默认为 gpt-3.5-turbo）
OPENAI_MODEL=gpt-3.5-turbo

# Temperature（可选，默认为 0.7）
OPENAI_TEMPERATURE=0.7

# 嵌入模型（可选，默认为 text-embedding-ada-002）
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# ==================== 其他 LLM 配置 ====================
# Azure OpenAI（仅 Multi-LLM-Provider 项目需要）
AZURE_OPENAI_API_KEY=your-azure-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-35-turbo

# Anthropic Claude（仅 Multi-LLM-Provider 项目需要）
ANTHROPIC_API_KEY=sk-ant-your-api-key

# Google Gemini（仅 Multi-LLM-Provider 项目需要）
VERTEX_AI_PROJECT_ID=your-gcp-project-id
VERTEX_AI_LOCATION=us-central1

# Ollama（仅 Multi-LLM-Provider 项目需要）
OLLAMA_BASE_URL=http://localhost:11434

# ==================== 数据库配置 ====================
# PostgreSQL（仅 RAG-Demo 使用 PGVector 时需要）
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vectordb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ==================== 应用配置 ====================
# 服务器端口（可选，默认为 8080）
SERVER_PORT=8080

# 日志级别（可选）
LOG_LEVEL_AI=INFO
LOG_LEVEL_APP=INFO

# 向量存储类型（仅 RAG-Demo 需要）
# 可选值：simple（内存）或 pgvector（PostgreSQL）
VECTOR_STORE_TYPE=simple
