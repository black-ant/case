package com.example.alldeps;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring AI 完整依赖展示应用
 * 
 * 注意：这个应用包含了所有 Spring AI 依赖，仅用于展示和学习目的。
 * 在实际生产环境中，应该根据需求选择特定的依赖。
 */
@SpringBootApplication
public class AllDependenciesDemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(AllDependenciesDemoApplication.class, args);
    }
}
