# Function Calling Demo - 工具调用与函数集成

这个项目深入展示了 Spring AI 的 Function Calling（函数调用）功能，让 AI 能够智能地调用 Java 函数来完成复杂任务。

## 🎯 什么是 Function Calling？

Function Calling 允许 AI 模型：
1. **理解用户意图**：识别用户需要什么功能
2. **选择合适的工具**：从可用函数中选择最合适的
3. **提取参数**：从用户输入中提取函数所需的参数
4. **执行函数**：调用 Java 函数获取实际数据
5. **生成回答**：基于函数返回结果生成自然语言回答

## 🚀 核心功能

### 1. 三个示例函数

#### ☀️ 天气查询函数
```java
@Bean
@Description("获取指定城市的天气信息，包括温度、湿度和天气状况")
public WeatherFunction weatherFunction() {
    return new WeatherFunction();
}
```

**AI 会自动识别**：
- "北京今天天气怎么样？"
- "上海的温度是多少？"
- "深圳会下雨吗？"

#### 📈 股票价格查询函数
```java
@Bean
@Description("查询指定股票代码的实时价格和涨跌幅信息")
public StockPriceFunction stockPriceFunction() {
    return new StockPriceFunction();
}
```

**AI 会自动识别**：
- "苹果股票现在多少钱？"
- "GOOGL 的股价是多少？"
- "微软股票涨了还是跌了？"

#### 🔢 计算器函数
```java
@Bean
@Description("执行基本的数学运算，支持加减乘除")
public CalculatorFunction calculatorFunction() {
    return new CalculatorFunction();
}
```

**AI 会自动识别**：
- "123 加 456 等于多少？"
- "1000 除以 25 是多少？"
- "帮我算一下 89 乘以 67"

### 2. 六种调用模式

| 模式 | 说明 | 使用场景 |
|------|------|----------|
| **单个函数** | 指定一个函数供 AI 使用 | 明确知道需要哪个功能 |
| **多函数选择** | AI 从多个函数中自动选择 | 让 AI 智能决策 |
| **自动调用** | 注册所有函数，AI 自由选择 | 最灵活的方式 |
| **强制调用** | 强制 AI 必须调用指定函数 | 确保函数被执行 |
| **链式调用** | AI 连续调用多个函数 | 复杂任务分解 |
| **上下文调用** | 带上下文信息的函数调用 | 需要背景信息 |

## 📋 快速开始

### 1. 配置 API Key

```bash
# Windows
set OPENAI_API_KEY=sk-xxx

# Linux/Mac
export OPENAI_API_KEY=sk-xxx
```

### 2. 运行项目

```bash
mvn spring-boot:run
```

### 3. 测试接口

#### 示例 1：天气查询
```bash
curl -X POST http://localhost:8080/api/function/auto \
  -H "Content-Type: application/json" \
  -d '{
    "message": "北京今天天气怎么样？"
  }'
```

**AI 执行流程**：
1. 识别用户想查询天气
2. 选择 `weatherFunction`
3. 提取参数：`city="北京"`
4. 调用函数获取天气数据
5. 生成自然语言回答

**响应示例**：
```
北京今天晴天，温度25℃，湿度60%。天气不错，适合外出活动。
```

#### 示例 2：股票查询
```bash
curl -X POST http://localhost:8080/api/function/auto \
  -H "Content-Type: application/json" \
  -d '{
    "message": "苹果公司的股票现在多少钱？"
  }'
```

**AI 执行流程**：
1. 识别用户想查询股票
2. 选择 `stockPriceFunction`
3. 提取参数：`symbol="AAPL"`
4. 调用函数获取股价
5. 生成回答

#### 示例 3：计算器
```bash
curl -X POST http://localhost:8080/api/function/auto \
  -H "Content-Type: application/json" \
  -d '{
    "message": "帮我算一下 123 加 456"
  }'
```

#### 示例 4：复杂查询（链式调用）
```bash
curl -X POST http://localhost:8080/api/function/chain \
  -H "Content-Type: application/json" \
  -d '{
    "message": "北京和上海今天哪个城市更热？温差是多少？"
  }'
```

**AI 执行流程**：
1. 调用 `weatherFunction("北京")`
2. 调用 `weatherFunction("上海")`
3. 调用 `calculatorFunction(temp1, temp2, "subtract")`
4. 综合结果生成回答

## 🏗️ 项目结构

```
Function-Calling-Demo/
├── src/
│   └── main/
│       ├── java/com/example/functioncalling/
│       │   ├── FunctionCallingDemoApplication.java  # 主启动类
│       │   ├── config/
│       │   │   └── FunctionConfig.java              # 函数注册配置
│       │   ├── controller/
│       │   │   └── FunctionCallingController.java   # REST 控制器
│       │   ├── service/
│       │   │   └── FunctionCallingService.java      # 函数调用服务
│       │   ├── functions/                           # 函数实现
│       │   │   ├── WeatherFunction.java             # 天气查询
│       │   │   ├── StockPriceFunction.java          # 股票查询
│       │   │   └── CalculatorFunction.java          # 计算器
│       │   └── model/                               # 数据模型
│       │       ├── WeatherRequest.java
│       │       └── WeatherResponse.java
│       └── resources/
│           └── application.yml                      # 配置文件
└── pom.xml                                          # Maven 配置
```

## 💡 如何创建自定义函数

### 步骤 1：创建函数类

```java
@Slf4j
public class MyCustomFunction implements Function<Request, Response> {
    
    @Data
    public static class Request {
        @JsonProperty(required = true)
        @JsonPropertyDescription("参数说明，AI 会读取这个描述")
        private String param1;
    }
    
    @Data
    public static class Response {
        private String result;
    }
    
    @Override
    public Response apply(Request request) {
        log.info("执行自定义函数: {}", request);
        // 实现你的业务逻辑
        return new Response("结果");
    }
}
```

### 步骤 2：注册函数

```java
@Configuration
public class FunctionConfig {
    
    @Bean
    @Description("这个函数的功能描述，AI 会根据这个描述决定何时调用")
    public MyCustomFunction myCustomFunction() {
        return new MyCustomFunction();
    }
}
```

### 步骤 3：使用函数

```java
chatClient.prompt()
    .user("用户问题")
    .functions("myCustomFunction")  // 注册函数
    .call()
    .content();
```

## 🔑 关键注解说明

### @Description
```java
@Bean
@Description("函数功能描述")  // AI 用这个来理解函数用途
public WeatherFunction weatherFunction() {
    return new WeatherFunction();
}
```

### @JsonPropertyDescription
```java
@JsonPropertyDescription("参数说明")  // AI 用这个来理解参数含义
private String city;
```

### @JsonProperty
```java
@JsonProperty(required = true)  // 标记必需参数
private String city;
```

## 📡 API 接口详解

### 1. POST /api/function/single
单个函数调用

```bash
curl -X POST http://localhost:8080/api/function/single \
  -H "Content-Type: application/json" \
  -d '{
    "message": "北京天气",
    "functionName": "weatherFunction"
  }'
```

### 2. POST /api/function/multiple
多函数调用（AI 自动选择）

```bash
curl -X POST http://localhost:8080/api/function/multiple \
  -H "Content-Type: application/json" \
  -d '{
    "message": "北京天气怎么样？苹果股票多少钱？"
  }'
```

### 3. POST /api/function/auto
自动函数调用（推荐）

```bash
curl -X POST http://localhost:8080/api/function/auto \
  -H "Content-Type: application/json" \
  -d '{
    "message": "帮我查一下深圳的天气"
  }'
```

### 4. POST /api/function/force
强制函数调用

```bash
curl -X POST http://localhost:8080/api/function/force \
  -H "Content-Type: application/json" \
  -d '{
    "message": "告诉我信息",
    "functionName": "weatherFunction"
  }'
```

### 5. POST /api/function/chain
链式函数调用

```bash
curl -X POST http://localhost:8080/api/function/chain \
  -H "Content-Type: application/json" \
  -d '{
    "message": "北京和上海哪个更热？温差多少？"
  }'
```

### 6. POST /api/function/context
带上下文的函数调用

```bash
curl -X POST http://localhost:8080/api/function/context \
  -H "Content-Type: application/json" \
  -d '{
    "message": "那里的天气怎么样？",
    "context": "用户刚才提到了北京"
  }'
```

## 🎨 实际应用场景

### 1. 智能客服
```java
// 注册多个业务函数
.functions("queryOrder", "cancelOrder", "refund", "trackShipment")
```

用户："我想查一下我的订单状态"
→ AI 自动调用 `queryOrder` 函数

### 2. 数据分析助手
```java
.functions("queryDatabase", "generateChart", "exportReport")
```

用户："帮我分析一下上个月的销售数据"
→ AI 依次调用多个函数完成分析

### 3. 智能家居控制
```java
.functions("turnOnLight", "setTemperature", "playMusic")
```

用户："把客厅灯打开，温度调到 25 度"
→ AI 并行调用多个控制函数

### 4. 开发助手
```java
.functions("searchCode", "runTests", "deployApp")
```

用户："帮我找到处理用户登录的代码"
→ AI 调用代码搜索函数

## 🆕 动态函数注册

除了静态注册函数，Spring AI 还支持动态注册函数。详细说明请查看 [DYNAMIC_FUNCTIONS.md](./DYNAMIC_FUNCTIONS.md)

### 快速体验

```bash
# 1. 批量注册示例函数
curl -X POST http://localhost:8080/api/dynamic/register/batch

# 2. 查看已注册的函数
curl http://localhost:8080/api/dynamic/list

# 3. 使用动态函数对话
curl -X POST http://localhost:8080/api/dynamic/chat/all \
  -H "Content-Type: application/json" \
  -d '{"message": "现在几点了？"}'

# 4. 注销函数
curl -X DELETE http://localhost:8080/api/dynamic/unregister/getCurrentTime
```

### 动态注册 API

| 接口 | 说明 |
|------|------|
| `POST /api/dynamic/register/example` | 注册预定义函数 |
| `POST /api/dynamic/register/batch` | 批量注册示例函数 |
| `GET /api/dynamic/list` | 列出所有函数 |
| `DELETE /api/dynamic/unregister/{name}` | 注销函数 |
| `POST /api/dynamic/chat` | 使用指定函数对话 |
| `POST /api/dynamic/chat/all` | 使用所有函数对话 |

## 🔧 高级技巧

### 1. 函数返回结构化数据
```java
public Response apply(Request request) {
    return new Response(
        data,           // 实际数据
        description,    // 给 AI 的描述
        metadata        // 元数据
    );
}
```

### 2. 错误处理
```java
public Response apply(Request request) {
    try {
        // 业务逻辑
    } catch (Exception e) {
        return new Response(null, "错误：" + e.getMessage());
    }
}
```

### 3. 异步函数调用
```java
@Bean
public Function<Request, CompletableFuture<Response>> asyncFunction() {
    return request -> CompletableFuture.supplyAsync(() -> {
        // 耗时操作
        return new Response();
    });
}
```

### 4. 函数组合
```java
chatClient.prompt()
    .user(message)
    .functions("function1", "function2", "function3")
    .call()
    .content();
```

## 📊 调试技巧

### 查看函数调用日志
```yaml
logging:
  level:
    org.springframework.ai: DEBUG
    com.example: DEBUG
```

### 监控函数执行
```java
@Override
public Response apply(Request request) {
    log.info("函数被调用: {}", request);
    long start = System.currentTimeMillis();
    
    Response response = // 执行逻辑
    
    log.info("函数执行完成，耗时: {}ms", System.currentTimeMillis() - start);
    return response;
}
```

## 🚨 注意事项

1. **函数描述要清晰**：AI 依赖 `@Description` 来理解函数用途
2. **参数说明要详细**：使用 `@JsonPropertyDescription` 说明每个参数
3. **返回值要有意义**：返回的数据应该是 AI 能理解的
4. **处理异常情况**：函数应该优雅地处理错误
5. **性能考虑**：函数执行不应该太慢，避免超时
6. **安全性**：验证输入参数，防止注入攻击

## 🔗 参考资料

- [Spring AI Function Calling 文档](https://docs.spring.io/spring-ai/reference/api/functions.html)
- [OpenAI Function Calling 指南](https://platform.openai.com/docs/guides/function-calling)
- [JSON Schema 规范](https://json-schema.org/)

## 📝 总结

Function Calling 是 Spring AI 最强大的功能之一，它让 AI 能够：

✅ **理解意图**：从自然语言中提取用户需求  
✅ **智能决策**：自动选择合适的工具  
✅ **执行操作**：调用真实的 Java 函数  
✅ **生成回答**：基于实际数据生成自然语言  

通过 Function Calling，你可以构建真正智能的应用，让 AI 不仅能聊天，还能实际完成任务！
